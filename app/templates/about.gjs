import RouteTemplate from "ember-route-template";

export default RouteTemplate(<template>
  Nothing going on here. ChartJs component should be released from memory when entering this route.
</template>);
